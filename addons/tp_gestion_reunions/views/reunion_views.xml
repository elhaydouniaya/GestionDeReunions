<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <!-- Action -->
    <record id="action_tp_reunion" model="ir.actions.act_window">
        <field name="name">Réunions TP</field>
        <field name="res_model">tp.reunion</field>
        <field name="view_mode">tree,form</field>
    </record>

    <!-- Menu principal -->
    <menuitem id="menu_tp_reunion_root" name="Réunions TP" sequence="10"/>

    <!-- Sous-menu -->
    <menuitem id="menu_tp_reunions"
              name="Gestion des Réunions"
              parent="menu_tp_reunion_root"
              action="action_tp_reunion"
              sequence="10"/>

    <!-- Vue liste -->
    <record id="view_tp_reunion_tree" model="ir.ui.view">
        <field name="name">tp.reunion.tree</field>
        <field name="model">tp.reunion</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name"/>
                <field name="date"/>
                <field name="lieu"/>
                <field name="responsable"/>
                <field name="statut"/>
            </tree>
        </field>
    </record>

    <!-- Vue formulaire -->
    <record id="view_tp_reunion_form" model="ir.ui.view">
        <field name="name">tp.reunion.form</field>
        <field name="model">tp.reunion</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="responsable"/>
                            <field name="date"/>
                        </group>
                        <group>
                            <field name="lieu"/>
                            <field name="statut"/>
                        </group>
                    </group>
                    <group>
                        <field name="description"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

</odoo>
